import 'package:flutter/material.dart';

class NR36 extends StatelessWidget {
  const NR36({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}