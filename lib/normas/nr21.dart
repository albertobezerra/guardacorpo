import 'package:flutter/material.dart';

class NR21 extends StatelessWidget {
  const NR21({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
