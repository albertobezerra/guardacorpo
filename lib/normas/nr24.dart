import 'package:flutter/material.dart';

class NR24 extends StatelessWidget {
  const NR24({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
