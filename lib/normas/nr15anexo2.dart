import 'package:flutter/material.dart';

class NR15Anexo2 extends StatelessWidget {
  const NR15Anexo2({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}