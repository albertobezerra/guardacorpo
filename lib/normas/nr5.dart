import 'package:flutter/material.dart';

class NR5 extends StatelessWidget {
  const NR5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
