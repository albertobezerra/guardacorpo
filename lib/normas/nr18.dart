import 'package:flutter/material.dart';

class NR18 extends StatelessWidget {
  const NR18({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}