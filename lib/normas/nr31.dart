import 'package:flutter/material.dart';

class NR31 extends StatelessWidget {
  const NR31({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}