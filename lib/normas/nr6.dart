import 'package:flutter/material.dart';

class NR6 extends StatelessWidget {
  const NR6({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}