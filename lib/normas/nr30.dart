import 'package:flutter/material.dart';

class NR30 extends StatelessWidget {
  const NR30({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}