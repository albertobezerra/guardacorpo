import 'package:flutter/material.dart';

class NR22 extends StatelessWidget {
  const NR22({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
