import 'package:flutter/material.dart';

class NR2 extends StatelessWidget {
  const NR2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
