import 'package:flutter/material.dart';

class NR20 extends StatelessWidget {
  const NR20({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}