import 'package:flutter/material.dart';

class NR1 extends StatelessWidget {
  const NR1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
