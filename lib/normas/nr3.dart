import 'package:flutter/material.dart';

class NR3 extends StatelessWidget {
  const NR3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
