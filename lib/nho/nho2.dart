import 'package:flutter/material.dart';

class NHO2 extends StatelessWidget {
  const NHO2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
