import 'package:flutter/material.dart';

class NormasEstacionariosAll extends StatelessWidget {
  const NormasEstacionariosAll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Fedeu aqui!'),
    );
  }
}
