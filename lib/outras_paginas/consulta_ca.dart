import 'package:flutter/material.dart';

class ConsultaCa extends StatelessWidget {
  const ConsultaCa({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}