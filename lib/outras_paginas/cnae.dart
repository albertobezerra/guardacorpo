import 'package:flutter/material.dart';

class CNAE extends StatelessWidget {
  const CNAE({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}