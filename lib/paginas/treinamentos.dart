import 'package:flutter/material.dart';

class Treinamentos extends StatelessWidget {
  const Treinamentos({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}