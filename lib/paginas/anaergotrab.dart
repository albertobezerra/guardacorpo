import 'package:flutter/material.dart';

class AnaErgTrab extends StatelessWidget {
  const AnaErgTrab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container());
  }
}
