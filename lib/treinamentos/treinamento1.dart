import 'package:flutter/material.dart';

class Treinamento1 extends StatelessWidget {
  const Treinamento1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
