import 'package:flutter/material.dart';

class Estacionario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Segunda Tela"),
    );
  }
}
